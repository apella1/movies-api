spring:
  application:
    name: movies
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    generate-ddl: true
  profiles:
    active: ${PROFILE:dev}

server:
  servlet:
    context-path: /api/v1

springdoc:
  show-actuator: true